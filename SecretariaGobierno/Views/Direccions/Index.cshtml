@model IEnumerable<SecretariaGobierno.Models.Direccion>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Establecimiento.EstablecimientoID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Direcciones)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Establecimiento.EstablecimientoID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Direcciones)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DireccionID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DireccionID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DireccionID })
        </td>
    </tr>
}

</table>
